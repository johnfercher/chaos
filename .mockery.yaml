all: True
dir: "mocks/"
outpkg: "mocks"
filename: "{{.InterfaceName}}.go"
mockname: "{{.InterfaceName}}"
with-expecter: True
packages:
  github.com/johnfercher/chaos/internal:
    config:
      recursive: True